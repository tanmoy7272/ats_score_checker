[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['cd server && npm install']

[phases.build]
cmds = ['cd server && npm run build || echo "No build step needed"']

[start]
cmd = 'node server/server.js'
